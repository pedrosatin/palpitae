{
  "name": "palpitae-api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --config ./wrangler.toml --port 8787",
    "dev:cron": "wrangler dev --config ./wrangler.toml --test-scheduled --port 8787",
    "deploy": "wrangler deploy --config ./wrangler.toml",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "hono": "^4.12.25",
    "hono-rate-limiter": "^0.5.3"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260503.1",
    "typescript": "^5.9.3",
    "vitest": "^4.1.8",
    "wrangler": "^4.100.0"
  },
  "overrides": {
    "esbuild": "0.28.1"
  }
}
