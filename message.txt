🧹 [false positive] Issue requests test for covered catch block

🎯 **What:**
The task highlighted a 'potentially untested error path' for the `catch (err)` block in `api/src/radar/sync.ts:177` (the try/catch wrapping `fetchPageviews`). I investigated the codebase to implement missing test coverage for this error path.

💡 **Why:**
Upon examining the test suite (`api/src/radar/sync.test.ts`), I verified that the error path is *already explicitly tested*. The test case named `não deixa artigo quebrado derrubar o snapshot` correctly mocks a rejection from `fetchPageviews` (`pageviewsMock.mockRejectedValue(new Error('boom'))`) and asserts that the error does not break the snapshot and the `partial` status is logged.

✅ **Verification:**
I ran the full test suite (`cd api && npm run test -- --run`). All 452 tests passed perfectly without any modifications to the code. There are no missing tests for this error path.

✨ **Result:**
No code changes were necessary as the reported issue is a false positive. Test coverage for the `catch` block is already fully implemented and functioning correctly.
