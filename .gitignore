node_modules/
dist/
.env
.env.local
\*.log

# graphify roda na raiz — saída acidental dentro de api/ não entra no repo
api/graphify-out/

# wrangler files
.wrangler
.dev.vars*
!.dev.vars.example
!.env.example

# context.md da raiz é scratch por tarefa (muda a cada atividade) — não versionar.
# Docs duráveis: docs/ (decisões em docs/architecture/decisions.md).
/context.md
